# OS / editor cruft
.DS_Store
Thumbs.db
*~
.vscode/
.idea/

# Python (only the image generator runs locally)
__pycache__/
*.pyc
.venv/

# Node (not used to build; never commit if added later)
node_modules/

# Local server / scratch
*.log
